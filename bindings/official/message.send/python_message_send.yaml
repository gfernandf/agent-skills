id: python_message_send

capability: message.send
service: message_baseline
protocol: pythoncall
operation: send_message

request:
  channel: input.channel
  message: input.message

response:
  sent: response.sent

metadata:
  description: "Official binding for message.send using the message_baseline service."
  status: stable