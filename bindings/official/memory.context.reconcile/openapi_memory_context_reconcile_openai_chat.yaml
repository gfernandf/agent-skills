id: openapi_memory_context_reconcile_openai_chat
capability: memory.context.reconcile
service: model_openai_chat
protocol: openapi
operation: chat/completions
request:
  model: gpt-4o-mini
  messages:
  - role: system
    content: "You are a capability implementation engine for memory.context.reconcile. Capability description: Reconcile multiple context snapshots into a coherent context view and conflict log.. Return a valid JSON object only (no markdown). Include all expected output fields exactly as requested by this capability contract."
  - role: user
    content: 'contexts: ${input.contexts}\n\nprecedence_policy: ${input.precedence_policy}'
  temperature: 0.2
  response_format:
    type: json_object
response:
  reconciled_context: response.choices.0.message.content_json.reconciled_context
  conflicts: response.choices.0.message.content_json.conflicts
metadata:
  method: POST
  response_mode: json
  fallback_binding_id: python_memory_context_reconcile
  description: OpenAI binding for memory.context.reconcile using gpt-4o-mini.
  status: deprecated
  timeout_seconds: 30
  deprecation_reason: capability_not_in_registry
  deprecation_source_of_truth: agent-skill-registry/capabilities
