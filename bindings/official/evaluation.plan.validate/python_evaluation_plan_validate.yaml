id: python_evaluation_plan_validate
capability: evaluation.plan.validate
service: cognitive_baseline
protocol: pythoncall
operation: evaluation_plan_validate
request:
  expanded_plan: input.expanded_plan
  registry: input.registry
  state_schema: input.state_schema
response:
  validation_result: response.validation_result
  validation_errors: response.validation_result.errors
  validation_warnings: response.validation_result.warnings
  repairable: response.validation_result.repairable
metadata:
  description: Python fallback for evaluation.plan.validate using cognitive_baseline service.
  status: stable
