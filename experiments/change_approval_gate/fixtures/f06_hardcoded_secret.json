{
  "_meta": {
    "fixture_id": "f06",
    "description": "PR that adds a hardcoded API key in a config file — label 'hardcoded-secret' present",
    "expected_decision": "block",
    "expected_risk_level": "critical",
    "violated_rules": ["Forbidden key present: hardcoded-secret"],
    "teaching_point": "Pattern-based auto-block. The label 'hardcoded-secret' is a forbidden pattern in the policy profile. The decision does not require semantic analysis — the policy gate fires immediately on the forbidden label. This is the fastest, most reliable class of block.",
    "policy": "strict_prod"
  },
  "change_package": {
    "change_id": "pr-0601",
    "change_type": "pr",
    "title": "Add Stripe API key to payment service config",
    "diff_text": "--- a/src/payments/config.py\n+++ b/src/payments/config.py\n@@ -4,3 +4,4 @@\n PAYMENT_GATEWAY = 'stripe'\n+STRIPE_API_KEY = 'sk_live_REDACTED_EXAMPLE_DO_NOT_USE'\n CURRENCY = 'USD'",
    "target_environment": "prod",
    "changed_files": ["src/payments/config.py"],
    "author_summary": "Adding Stripe live key directly to config for now, will move to secrets manager in follow-up.",
    "labels": ["payments", "hardcoded-secret", "security"],
    "test_evidence": "Manual smoke test: payment flow works end-to-end.",
    "rollback_plan": "Revert PR. Remove key from source — coordinate with security to rotate the key immediately."
  }
}
