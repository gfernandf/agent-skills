id: python_agent_task_plan
capability: agent.task.plan
service: agent_baseline
protocol: pythoncall
operation: create_macro_plan
request:
  interpreted_goal: input.interpreted_goal
  candidate_skills: input.candidate_skills
  candidate_capabilities: input.candidate_capabilities
  planning_strategy: input.planning_strategy
response:
  macro_plan: response.macro_plan
  stage_count: response.stage_count
metadata:
  description: Python fallback for agent.task.plan using agent_baseline service.
  status: stable
