id: python_fs_read

capability: fs.read
service: fs_baseline
protocol: pythoncall
operation: read_file

request:
  path: input.path
  mode: input.mode

response:
  content: response.content
  bytes: response.bytes

metadata:
  description: "Official binding for fs.read using the fs_baseline service."
  status: stable