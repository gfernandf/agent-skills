"""
Video baseline service module.
Provides baseline implementations for video-related capabilities.
"""

def extract_frames(video_data, frame_rate):
    """
    Extract frames from video data.
    
    Args:
        video_data (bytes): The video data.
        frame_rate (int): The frame extraction rate.
    
    Returns:
        dict: {"frames": list}
    """
    # Baseline implementation: empty list
    return {"frames": []}