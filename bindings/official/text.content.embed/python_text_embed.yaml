id: python_text_embed

capability: reasoning.embedding.generate
service: text_baseline
protocol: pythoncall
operation: embed_text

request:
  text: input.text

response:
  embedding: response.embedding

metadata:
  description: "Official binding for text.content.embed using the text_baseline service."
  status: stable
