id: python_integration_connector_get
capability: integration.connector.get
service: integration_baseline
protocol: pythoncall
operation: get_connector

request:
  connector_id: input.connector_id

response:
  connector: response.connector
  found: response.found

metadata:
  description: Official pythoncall binding for integration.connector.get.
  status: experimental
