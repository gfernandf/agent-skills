id: python_ops_event_monitor
capability: ops.event.monitor
service: ops_baseline
protocol: pythoncall
operation: monitor_events

request:
  events: input.events
  thresholds: input.thresholds

response:
  status: response.status
  alerts: response.alerts
  event_summary: response.event_summary

metadata:
  description: Official pythoncall binding for ops.event.monitor.
  status: experimental
