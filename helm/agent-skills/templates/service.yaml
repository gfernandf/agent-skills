apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-skills.fullname" . }}
  labels:
    {{- include "agent-skills.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-skills.selectorLabels" . | nindent 4 }}
