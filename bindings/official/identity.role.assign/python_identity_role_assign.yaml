id: python_identity_role_assign
capability: identity.role.assign
service: identity_baseline
protocol: pythoncall
operation: assign_role

request:
  principal_id: input.principal_id
  role_id: input.role_id

response:
  assigned: response.assigned
  effective_permissions: response.effective_permissions

metadata:
  description: Official pythoncall binding for identity.role.assign.
  status: experimental
