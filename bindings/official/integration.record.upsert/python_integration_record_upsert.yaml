id: python_integration_record_upsert
capability: integration.record.upsert
service: integration_baseline
protocol: pythoncall
operation: upsert_record

request:
  connector_id: input.connector_id
  record: input.record

response:
  action: response.action
  record_id: response.record_id

metadata:
  description: Official pythoncall binding for integration.record.upsert.
  status: experimental
