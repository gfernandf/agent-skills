id: python_identity_permission_list
capability: identity.permission.list
service: identity_baseline
protocol: pythoncall
operation: list_permissions

request:
  principal_id: input.principal_id
  resource_filter: input.resource_filter

response:
  permissions: response.permissions
  total: response.total

metadata:
  description: Official pythoncall binding for identity.permission.list.
  status: experimental
