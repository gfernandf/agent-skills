id: mcp_integration_record_upsert_inprocess
capability: integration.record.upsert
service: baseline_mcp_inprocess
protocol: mcp
operation: integration.record.upsert
request:
  connector_id: input.connector_id
  record: input.record
response:
  action: response.action
  record_id: response.record_id
metadata:
  description: Official MCP fallback binding for integration.record.upsert using in-process
    baseline tools server.
  status: experimental
  fallback_binding_id: python_integration_record_upsert
