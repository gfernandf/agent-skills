id: python_policy_risk_score
capability: policy.risk.score
service: policy_baseline
protocol: pythoncall
operation: score_risk

request:
  action: input.action
  dimensions: input.dimensions

response:
  risk_score: response.risk_score
  dimension_scores: response.dimension_scores
  safe: response.safe

metadata:
  description: Official pythoncall binding for policy.risk.score.
  status: experimental
