id: python_text_template

capability: text.content.template
service: text_baseline
protocol: pythoncall
operation: template_text

request:
  template: input.template
  variables: input.variables

response:
  text: response.text

metadata:
  description: "Official binding for text.content.template using the text_baseline service."
  status: stable