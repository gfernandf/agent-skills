id: python_memory_context_summarize
capability: memory.context.summarize
service: cognitive_baseline
protocol: pythoncall
operation: memory_context_compress
request:
  context: input.context
  target_length: input.target_length
response:
  summary_context: response.summary_context
  summary_notes: response.summary_notes
metadata:
  description: Python fallback for memory.context.summarize using memory context compression baseline.
  status: stable
