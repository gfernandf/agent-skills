id: python_memory_retrieve

capability: memory.entry.retrieve
service: memory_baseline
protocol: pythoncall
operation: retrieve_memory

request:
  key: input.key

response:
  found: response.found
  value: response.value

metadata:
  description: "Official binding for memory.entry.retrieve using the memory_baseline service."
  status: stable