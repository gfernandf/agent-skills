id: python_perception_event_monitor
capability: perception.event.monitor
service: ops_baseline
protocol: pythoncall
operation: monitor_events
request:
  events: input.events
  thresholds: input.thresholds
response:
  status: response.status
  alerts: response.alerts
  event_summary: response.event_summary
metadata:
  description: Python fallback for perception.event.monitor using ops event monitoring baseline.
  status: stable
