id: python_reasoning_plan_reconcile
capability: reasoning.plan.reconcile
service: cognitive_baseline
protocol: pythoncall
operation: reasoning_plan_reconcile
request:
  invalid_plan: input.invalid_plan
  validation_errors: input.validation_errors
  registry: input.registry
response:
  repaired_plan: response.repaired_plan
  repair_notes: response.repair_notes
  still_invalid: response.still_invalid
metadata:
  description: Python fallback for reasoning.plan.reconcile using cognitive_baseline service.
  status: stable
