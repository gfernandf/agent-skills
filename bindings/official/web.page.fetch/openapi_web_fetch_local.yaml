id: openapi_web_fetch_local

capability: web.page.fetch
service: web_fetch_openapi_local
protocol: openapi
operation: fetch

request:
  url: input.url
  timeout: input.timeout
  headers: input.headers

response:
  result: response.result
  status: response.status
  trace_ref: response.trace_ref

metadata:
  method: POST
  description: "Local OpenAPI pilot binding for web.page.fetch."
  status: pilot
  timeout_seconds: 30
