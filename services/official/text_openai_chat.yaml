id: text_openai_chat
kind: openapi
base_url: https://api.openai.com/v1

metadata:
  description: "Official OpenAI Chat Completions service for text capabilities."
  maintained_by: "agent-skills"
  status: experimental
  timeout_seconds: 45
  headers:
    Authorization: "Bearer ${OPENAI_API_KEY}"
    Content-Type: "application/json"
