id: mcp_integration_record_create_inprocess
capability: integration.record.create
service: baseline_mcp_inprocess
protocol: mcp
operation: integration.record.create
request:
  connector_id: input.connector_id
  record: input.record
response:
  created: response.created
  record_id: response.record_id
metadata:
  description: Official MCP fallback binding for integration.record.create using in-process
    baseline tools server.
  status: experimental
  fallback_binding_id: python_integration_record_create
