id: python_agent_plan_generate
capability: agent.plan.generate
service: agent_baseline
protocol: pythoncall
operation: generate_plan
request:
  goal: input.objective
  context: input.context
  max_steps: input.max_steps
response:
  plan: response.plan
  step_count: response.step_count
metadata:
  description: Official binding for agent.plan.generate using the agent_baseline service.
  status: stable
