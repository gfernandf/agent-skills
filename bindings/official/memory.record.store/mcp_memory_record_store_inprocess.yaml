id: mcp_memory_record_store_inprocess
capability: memory.record.store
service: baseline_mcp_inprocess
protocol: mcp
operation: memory.record.store
request:
  namespace: input.namespace
  record: input.record
  ttl_seconds: input.ttl_seconds
response:
  stored: response.stored
  record_id: response.record_id
metadata:
  description: Official MCP fallback binding for memory.record.store using in-process
    baseline tools server.
  status: experimental
  fallback_binding_id: python_memory_record_store
