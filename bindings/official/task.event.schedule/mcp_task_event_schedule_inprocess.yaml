id: mcp_task_event_schedule_inprocess
capability: task.event.schedule
service: baseline_mcp_inprocess
protocol: mcp
operation: task.event.schedule
request:
  title: input.title
  description: input.description
  scheduled_time: input.scheduled_time
  recurrence: input.recurrence
  attendees: input.attendees
  context: input.context
response:
  event_id: response.event_id
  scheduled: response.scheduled
  scheduled_time: response.scheduled_time
metadata:
  description: Official MCP fallback binding for task.event.schedule using in-process
    baseline tools server.
  status: experimental
  fallback_binding_id: python_task_event_schedule
