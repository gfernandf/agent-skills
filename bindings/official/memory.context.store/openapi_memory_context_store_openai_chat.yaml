id: openapi_memory_entry_store_openai_chat
capability: memory.context.store
service: model_openai_chat
protocol: openapi
operation: chat/completions
request:
  model: gpt-4o-mini
  messages:
  - role: system
    content: "You are a capability implementation engine for memory.entry.store. Capability description: Store a key-value memory entry. Return a valid JSON object only (no markdown). Include all expected output fields exactly as requested by this capability contract."
  - role: user
    content: 'key: ${input.key}\n\nvalue: ${input.value}'
  temperature: 0.2
  response_format:
    type: json_object
response:
  stored:
  - response.choices.0.message.content_json.stored
  - response.choices.0.message.content_json.success
  - response.choices.0.message.content_json.saved
  - response.choices.0.message.content_json.status
metadata:
  method: POST
  response_mode: json
  fallback_binding_id: python_memory_entry_store
  description: OpenAI binding for memory.entry.store using gpt-4o-mini.
  status: deprecated
  timeout_seconds: 30
