id: python_code_format

capability: code.source.format
service: code_baseline
protocol: pythoncall
operation: format_code

request:
  code: input.code
  language: input.language

response:
  formatted_code: response.formatted_code

metadata:
  description: "Official binding for code.source.format using the code_baseline service."
  status: stable