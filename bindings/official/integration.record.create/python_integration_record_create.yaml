id: python_integration_record_create
capability: integration.record.create
service: integration_baseline
protocol: pythoncall
operation: create_record

request:
  connector_id: input.connector_id
  record: input.record

response:
  created: response.created
  record_id: response.record_id

metadata:
  description: Official pythoncall binding for integration.record.create.
  status: experimental
