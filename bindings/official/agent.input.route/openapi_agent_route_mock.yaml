id: openapi_agent_route_mock

capability: agent.input.route
service: agent_route_openapi_mock
protocol: openapi
operation: route

request:
  query: input.query
  agents: input.agents

response:
  route: response.route

metadata:
  method: POST
  description: "OpenAPI mock binding for agent.input.route."
  status: experimental
