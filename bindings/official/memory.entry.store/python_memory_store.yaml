id: python_memory_store

capability: memory.entry.store
service: memory_baseline
protocol: pythoncall
operation: store_memory

request:
  key: input.key
  value: input.value

response:
  stored: response.stored

metadata:
  description: "Official binding for memory.entry.store using the memory_baseline service."
  status: stable