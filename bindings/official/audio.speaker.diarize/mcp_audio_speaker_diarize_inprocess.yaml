id: mcp_audio_speaker_diarize_inprocess
capability: audio.speaker.diarize
service: baseline_mcp_inprocess
protocol: mcp
operation: audio.speaker.diarize
request:
  audio: input.audio
  max_speakers: input.max_speakers
response:
  segments: response.segments
  speaker_count: response.speaker_count
metadata:
  description: Official MCP fallback binding for audio.speaker.diarize using in-process
    baseline tools server.
  status: experimental
  fallback_binding_id: python_audio_speaker_diarize
