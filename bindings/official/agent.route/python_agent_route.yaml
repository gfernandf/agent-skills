id: python_agent_route
capability: agent.route
service: agent_baseline
protocol: pythoncall
operation: route_agent
request:
  query: input.input
  agents: input.input
response:
  selected_agent: response.selected_agent
metadata:
  description: Official binding for agent.route using the agent_baseline service.
  status: stable
