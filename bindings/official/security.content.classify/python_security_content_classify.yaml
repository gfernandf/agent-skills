id: python_security_content_classify
capability: security.content.classify
service: security_baseline
protocol: pythoncall
operation: classify_content

request:
  payload: input.payload
  context: input.context

response:
  categories: response.categories
  sensitivity_level: response.sensitivity_level
  category_scores: response.category_scores
  rationale: response.rationale
  evidence: response.evidence

metadata:
  description: Official pythoncall binding for security.content.classify.
  status: experimental
