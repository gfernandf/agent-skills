id: mcp_agent_flow_wait_inprocess
capability: agent.flow.wait
service: baseline_mcp_inprocess
protocol: mcp
operation: agent.flow.wait
request:
  condition: input.condition
  timeout_seconds: input.timeout_seconds
  poll_interval_seconds: input.poll_interval_seconds
response:
  resolved: response.resolved
  event: response.event
  timed_out: response.timed_out
  elapsed_seconds: response.elapsed_seconds
metadata:
  description: Official MCP fallback binding for agent.flow.wait using in-process
    baseline tools server.
  status: experimental
  fallback_binding_id: python_agent_flow_wait
