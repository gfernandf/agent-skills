id: python_integration_record_update
capability: integration.record.update
service: integration_baseline
protocol: pythoncall
operation: update_record

request:
  connector_id: input.connector_id
  record_id: input.record_id
  fields: input.fields

response:
  updated: response.updated

metadata:
  description: Official pythoncall binding for integration.record.update.
  status: experimental
